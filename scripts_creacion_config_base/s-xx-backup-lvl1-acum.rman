
RUN {
    
    BACKUP AS BACKUPSET INCREMENTAL LEVEL 1 CUMULATIVE DATABASE PLUS ARCHIVELOG TAG 'LVL1_CUM_FRA';

    BACKUP ARCHIVELOG ALL DELETE INPUT TAG 'ARCH_FRA_DIFF';

    DELETE NOPROMPT OBSOLETE;
}

EXIT;